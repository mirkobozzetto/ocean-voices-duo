// * Padding interactif
const computePadding = (open) =>
  `padding-top: ${open ? "10rem" : "4rem"} + 5vh`;
window.computePadding = computePadding;
//______________________________________________________________________________

// * Gestion du saut de lignes
